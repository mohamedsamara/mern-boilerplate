import React, { useEffect } from 'react';

const Dashboard = () => {
  useEffect(() => {
    // if (state.user)  {
    // fetchUser();
    // }
  }, []);

  return <h1>Dashboard</h1>;
};

export default Dashboard;
